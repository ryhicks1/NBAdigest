{
  "installCommand": "echo 'no install needed'",
  "buildCommand": "mkdir -p dist/data && cp docs/index.html docs/style.css docs/app.js docs/robots.txt docs/sitemap.xml docs/llms.txt dist/ && cp data/anomalies.json dist/data/ && cp docs/CNAME dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "framework": null
}
